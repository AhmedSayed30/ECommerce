<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MainActivityTheme" parent="Theme.SplashScreen">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="windowSplashScreenBackground">@color/blue</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/avd_anim</item>
        <item name="postSplashScreenTheme">@style/Theme.ECommerce</item>
        <item name="windowSplashScreenAnimationDuration">3000</item>
        <item name="android:statusBarColor">@color/blue</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>